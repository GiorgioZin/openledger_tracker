# Loaded automatically by `npm run demo` (vite --mode demo).
# Runs the whole app against an in-memory store with seeded sample data —
# no Supabase, no network. Nothing here is secret.
VITE_DEMO=true
